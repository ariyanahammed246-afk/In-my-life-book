<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>আমাদের বইয়ের গ্রুপ</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f4f1ea; color: #333; margin: 0; padding: 20px; }
        .container { max-width: 800px; margin: auto; background: white; padding: 30px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        h1 { color: #5d4037; border-bottom: 2px solid #d7ccc8; padding-bottom: 10px; }
        .current-book { background: #efebe9; padding: 20px; border-radius: 10px; margin-bottom: 20px; border-left: 5px solid #8d6e63; }
        .section-title { font-weight: bold; color: #795548; margin-top: 20px; display: block; }
        ul { list-style: none; padding: 0; }
        li { background: #fff; margin: 5px 0; padding: 10px; border: 1px solid #ddd; border-radius: 5px; display: flex; justify-content: space-between; }
        .btn { background: #8d6e63; color: white; border: none; padding: 10px 15px; cursor: pointer; border-radius: 5px; transition: 0.3s; }
        .btn:hover { background: #5d4037; }
        input { padding: 8px; border: 1px solid #ccc; border-radius: 5px; width: 70%; }
    </style>
</head>
<body>

<div class="container">
    <h1>📚 শব্দকুটির বুক ক্লাব</h1>
    
    <div class="current-book">
        <h3>📖 এই মাসের বই: <span id="bookName">পদ্মা নদীর মাঝি</span></h3>
        <p>লেখক: মানিক বন্দ্যোপাধ্যায়</p>
        <p><strong>মিটিংয়ের তারিখ:</strong> ২৫শে অক্টোবর, ২০২৪</p>
    </div>

    <span class="section-title">👥 সদস্যবৃন্দ</span>
    <ul id="memberList">
        <li>আরিফ হোসেন <span>সদস্য</span></li>
        <li>সুমাইয়া রহমান <span>সদস্য</span></li>
    </ul>

    <div style="margin-top: 20px;">
        <input type="text" id="memberName" placeholder="নতুন সদস্যের নাম...">
        <button class="btn" onclick="addMember()">যোগ করুন</button>
    </div>
</div>

<script>
    function addMember() {
        var name = document.getElementById("memberName").value;
        if (name === "") {
            alert("দয়া করে নাম লিখুন!");
            return;
        }
        var li = document.createElement("li");
        li.innerHTML = name + " <span>সদস্য</span>";
        document.getElementById("memberList").appendChild(li);
        document.getElementById("memberName").value = "";
    }
</script>

</body>
</html>
